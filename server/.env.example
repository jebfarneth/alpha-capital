# Supabase (same instance as SaaS — copy values from ~/buttonwood/server/.env)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# FMP Professional (provision separately from SaaS Starter key)
FMP_API_KEY=

# Alpaca (paper trading initially; switch base URL to live when ready)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# Server
PORT=5002
NODE_ENV=development
